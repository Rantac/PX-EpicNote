export interface EpicNote {
  id: string;
  content: string;
  createdAt: string;
}

export interface AnalysisNote {
  id: string;
  title?: string;
  summary: string;
  createdAt: string;
}

export interface CryptoNote {
  id: string;
  content: string;
  createdAt: string;
}
