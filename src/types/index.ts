export interface EpicNote {
  id: string;
  content: string;
  createdAt: string;
}

export interface AnalysisNote {
  id: string;
  summary: string;
  createdAt: string;
}

export interface CryptoNote {
  id: string;
  content: string;
  createdAt: string;
}
